# Runtime dependencies for the Streamlit demo (used by Streamlit Community Cloud).
# For local development, prefer: pip install -r python_sim/requirements.txt
streamlit>=1.36,<2.0
pandas>=2.2,<3.0
# altair < 5.5 — newer altair uses PEP 728 TypedDict (closed=True) which crashes on Python 3.14
altair>=5.0,<5.5
